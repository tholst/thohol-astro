---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title="All this website are belong to Thomas."
      description={SITE_DESCRIPTION}
    />
    <style>
      main {
        width: 720px;
        max-width: calc(100% - 2em);
        margin: auto;
        padding: 3em 1em;
        color: var(--text-primary);
      }
      h1 {
        margin-bottom: 1em;
        color: var(--text-primary);
      }
      p {
        margin-bottom: 1.5em;
        color: var(--text-secondary);
      }
      a {
        color: var(--accent);
        text-decoration: none;
        transition: color 0.2s ease;
      }
      a:hover {
        color: var(--accent-dark);
      }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <h1>Software Development & stuff</h1>
      <p>
        Welcome to my personal notes about software development, the Web and
        Everything.
      </p>
      <p>
        <a href="/blog">Check out my blog posts</a> where I write about web technologies,
        React, Docker, and development practices.
      </p>
    </main>
    <Footer />
  </body>
</html>
